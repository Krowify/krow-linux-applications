# Deployed by 5-dotfiles.sh into ~/.config/hyde-themes/. Read by theme.sh
# for the two theme-independent bits of chrome -- cursor and the SDDM
# login theme stay the same across every desktop theme in this repo (see
# the README's theme switching section for why).

CURSOR_THEME_GLOB="Bibata-Modern-Classic*"
CURSOR_THEME_FALLBACK_GLOB="Bibata*"
SDDM_THEME_GLOB="pixel-sakura*"
